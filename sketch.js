function setup() {
  // creating canvas and the background
  createCanvas(1280, 1080);
  background(51);
}

function draw() {
 //color white for circle so it maintains its color
 fill(255, 255, 255);
 ellipse(150, 200, 250, 250);
 
 //Pyramid size, position, Color of pyramid
 fill(240, 230, 140);
 triangle(130, 1075, 658, 620, 1080, 1075);
 //line in pyramid
 line(780, 780, 780, 1060)
}
//Eric drove Chris typed

// and pravesh committed
